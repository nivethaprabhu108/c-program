#include <stdio.h>

int main() {
    int i;
    float rain[12], sum = 0;

    for (i = 0; i < 12; i++) {
        printf("Enter rainfall for month %d: ", i + 1);
        scanf("%f", &rain[i]);
        sum += rain[i];
    }

    printf("Average rainfall = %.2f", sum / 12);
    return 0;
}
