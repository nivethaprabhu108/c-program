#include <stdio.h>

int main() {
    int days, i;
    float sales[30], total = 0;

    printf("Enter number of days: ");
    scanf("%d", &days);

    for(i = 0; i < days; i++) {
        scanf("%f", &sales[i]);
        total += sales[i];
    }

    printf("Total Sales = %.2f\n", total);
    return 0;
}
