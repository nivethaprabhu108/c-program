#include <stdio.h>

struct Course {
    int courseId;
    char name[30];
    int duration;
};

int main() {
    struct Course c;

    printf("Enter Course ID: ");
    scanf("%d", &c.courseId);
    printf("Enter Course Name: ");
    scanf("%s", c.name);
    printf("Enter Duration (months): ");
    scanf("%d", &c.duration);

    printf("\nCourse Details:\n");
    printf("ID: %d\nName: %s\nDuration: %d months\n",
           c.courseId, c.name, c.duration);
    return 0;
}
