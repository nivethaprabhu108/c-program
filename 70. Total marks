#include <stdio.h>

struct Marks {
    int m1, m2, m3;
    int total;
};

int main() {
    struct Marks s;

    printf("Enter marks of 3 subjects:\n");
    scanf("%d %d %d", &s.m1, &s.m2, &s.m3);

    s.total = s.m1 + s.m2 + s.m3;

    printf("\nTotal Marks = %d\n", s.total);
    return 0;
}
